<%= rails_env %>:
  sessions:
    default:
      database: <%= mongo_database %>
      username: <%= mongo_user %>
      password: <%= mongo_password %>
      hosts:
        - <%= mongo_host %>:<%= mongo_port %>
      options:
        consistency: :strong
  options:
